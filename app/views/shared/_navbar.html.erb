<nav class="navbar navbar-expand-lg navbar-light bg-light">

  <div class="bs-offset-main bs-canvas-anim">
    <button class="btn btn-light" id="canvas-open" data-toggle="canvas" data-target="#bs-canvas-left" aria-expanded="false" aria-controls="bs-canvas-left">&#9776;</button>
  </div>

  <%= link_to('Apartment Alerts', root_path, class: 'navbar-brand') %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
  </button>
  
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
		<ul class="nav navbar-nav ml-auto">

		<% if user_signed_in? %>
			<li class="nav-item dropdown">
				<%= link_to(current_user.username, '#', id: 'NavbarDropdown', role: 'button', 'data-toggle': 'dropdown', aria: { haspopup: true, expanded: false}, class: 'nav-link dropdown-toggle') %>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <%= link_to('Edit Account', edit_user_registration_path, class: 'dropdown-item') unless current_user.guest? %>
          <%= link_to('log out',
                      destroy_user_session_path,
                      method: :delete,
                      class: 'dropdown-item') %>
        </div>
			</li>
    <% else %>
      <li class="nav-item">
        <%= link_to('Log in', user_session_path, class: 'nav-link') %>
      </li>
      <li class="nav-item">
        <%= link_to('Sign up', new_user_registration_path, class: 'active nav-link') %>
      </li>
    <% end %>
		</ul>
	</div>
</nav>
