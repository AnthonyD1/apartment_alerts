  <%= @new_posts.count %> new listing(s) have been found for "<%= @alert.name %>" in <%= @alert.city.titleize %>. Manage them at <%= alert_url(@alert) %>

<%= @new_posts.each_with_object(nil) do |post| %>
  <%= post.link %>
<% end %>
